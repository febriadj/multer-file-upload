<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <%- include ('./partials/nav.ejs'); %>
  <div id="content-img">
    <div id="content-img-wrap">
      <% images.map( img => { %>
        <div class="content-img-item">
          <img src="/<%= img.img %>" alt="">
          <p><%= img.deskripsi %></p>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html>